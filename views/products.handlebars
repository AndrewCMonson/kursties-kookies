<section>
    <h2 class='text-center'>Shop All of Our Products!</h2>
</section>
<section class="container">
    <div class="row row-cols-auto">
        {{#each products as |product|}}
            {{> product-details}}
        {{/each}}
    </div>
</section>