<div class="container">
    <div class="row"></div>
    <div class="col">
        <img class="img-fluid cookie-img"src="/images/{{filename}}" alt="{{product_name}}" />
    </div>
    <div class="col">
    {{product_name}}
    </div>
    <div class="col">
    {{CartItem.quantity}}
    </div>
    <div class="col">
        ${{calculate_total price CartItem.quantity}}
    </div>
        <button class="remove-from-cart" data-product-id="{{id}}">Remove from cart</button>
    </div>
</div>